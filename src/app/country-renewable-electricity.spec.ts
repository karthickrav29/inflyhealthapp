import { CountryRenewableElectricity } from './country-renewable-electricity';

describe('CountryRenewableElectricity', () => {
  it('should create an instance', () => {
    expect(new CountryRenewableElectricity()).toBeTruthy();
  });
});
